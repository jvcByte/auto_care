import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Rock of Ages Auto Technology',
  webDir: 'dist'
};

export default config;
